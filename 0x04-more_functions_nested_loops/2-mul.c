#include "main.h"

/**
 * mul - Multiplies two numbers
 * @a: The first number to multiply.
 * @b: The second number to multiply.
 *
 * Return: On success 1
 */
int mul(int a, int b)
{
	return (a * b);
}
