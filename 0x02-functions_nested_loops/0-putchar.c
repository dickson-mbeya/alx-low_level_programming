#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0(Success)
 */
int main (void)
{
	int i = 95;
	while (i == 95 || i == 112 || i ==  117 || i == 116 || i == 99 || i == 104 || i == 97 || i == 114)
	{
		_putchar(i);
		i++;
	}
	return (0);
}
